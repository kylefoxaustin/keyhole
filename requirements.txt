# Keyhole — Dependencies

# Core
python-dotenv>=1.0.0
click>=8.1.0
rich>=13.0.0
pydantic>=2.0.0

# Video Processing
ffmpeg-python>=0.2.0

# YOLO Detection
ultralytics>=8.3.0
torch>=2.2.0
torchvision>=0.17.0

# SAM 3
# NOTE: SAM 3 is installed from source — see scripts/download_models.sh
# Requires: einops, flash-attn-3, cc_torch
einops>=0.7.0
Pillow>=10.0.0
opencv-python>=4.9.0
pycocotools>=2.0.0

# Database
sqlalchemy>=2.0.0
alembic>=1.13.0

# API Server
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
jinja2>=3.1.0
python-multipart>=0.0.20

# LLM Backends
anthropic>=0.40.0
httpx>=0.27.0

# Semantic search (embeddings over detection descriptions)
sentence-transformers>=5.0.0
# Optional: GPU-native quantization (used only by experimental INT8 path)
torchao>=0.17.0

# Lightweight SAM variants for the Hybrid pipelines
# (installed from git, not pip — see README)
# mobile_sam  @ git+https://github.com/ChaoningZhang/MobileSAM.git
# efficient_sam @ git+https://github.com/yformer/EfficientSAM.git
open-clip-torch>=3.0.0

# GPU Profiling
nvidia-ml-py>=12.0.0
psutil>=5.9.0

# Deck + XLSX export (build_deck.py + export_results.py)
python-pptx>=1.0.0
matplotlib>=3.8.0
pandas>=2.2.0
openpyxl>=3.1.0

# ONNX export + TensorRT engine building (bakeoff_trt_yolo.py + bakeoff_trt_clip.py)
# NOTE: TensorRT must be installed manually — `pip install tensorrt` with the
# cu13 wheels matching your CUDA toolkit version. See REPRODUCE.md.
onnx>=1.18.0
onnxslim>=0.1.9
onnxscript>=0.6.0

# LLM bake-off (bakeoff_llm.py)
# NOTE: llama-cpp-python must be installed manually with CUDA build flags —
# see REPRODUCE.md.
huggingface-hub>=0.35.0

# Dev / Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
